# Application deadline

Applicants can submit strictly before the deadline.
At or after the deadline, submission must be rejected.
All dates must include a timezone. Naive datetimes are invalid inputs.

Scope: the deterministic deadline helper. API enforcement, permission checks,
tenant isolation and UI error presentation are outside this small example.
